'use client';

import React from 'react';
import CRowDivider from '@/common/components/layout/CRowDivider';
import dynamic from 'next/dynamic';

export default function DashboardToday(props: {
    drawData: any[];
    ticketIssue: any[];
    memberJoin: any[];
}) {
    return (
        <div className={'p-4 pt-16'}>
        </div>
    );
}
